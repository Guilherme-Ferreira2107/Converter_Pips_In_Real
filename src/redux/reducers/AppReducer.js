const INITIAL_STATE = {
    carregando: false,
    redirect:'', 
}

const AppReducer = (state = INITIAL_STATE, action) => {
    console.log(action);
    switch(action.type){
        }
    return state;
}
 
export default AppReducer;